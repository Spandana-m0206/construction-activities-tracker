# maguz-api

```bash
git clone <>
cd maguz-api
# make sure node and npm is install
# check: nvm version > node18
npm install

# to start mongodb
# make sure that docker is installed.
make mongo-up
make mongo-logs
make run
```

